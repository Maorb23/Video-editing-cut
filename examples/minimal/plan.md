# Minimal edit plan

Combine one ten-second source on the primary video track at 1920×1080 and
30000/1001 fps. Add a centered caption over frames 30–89, then export H.264/AAC
MP4. Replace `assets/input.mp4` and its metadata with values from
`probe_media.py` before validating this illustrative plan.

